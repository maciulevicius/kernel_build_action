name: Build Singularity HA-Docker Kernel (Android 16)

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential cpio flex git libelf-dev libssl-dev make python3 xz-utils curl zip python2-minimal

      - name: Clone Toolchain
        run: |
          git clone --depth 1 https://github.com/kdrag0n/proton-clang clang
          echo "$GITHUB_WORKSPACE/clang/bin" >> $GITHUB_PATH

      - name: Clone Singularity Kernel (Lineage-23.2 branch)
        run: |
          # Naudojame tavo nurodytą repozitoriją ir lineage-23.2 branch'ą
          git clone --depth 1 -b lineage-23.2 https://github.com/The-Anomalist/Singularity.git kernel

      - name: Patch Config (Docker & USB & LTO Fix)
        run: |
          cd kernel
          # Surandame konfigūracijos failą (jis gali būti oplus/ arba vendor/ aplanke)
          DEFCONFIG=$(find arch/arm64/configs/ -name "kona-perf_defconfig" | head -n 1)
          echo "Naudojamas defconfig: $DEFCONFIG"
          
          # 1. IŠJUNGIAME LTO (Sutvarko "geltoną užrašą" ir pakibimą)
          sed -i 's/CONFIG_LTO_CLANG=y/CONFIG_LTO_NONE=y/g' $DEFCONFIG
          sed -i 's/CONFIG_LTO=y/CONFIG_LTO_NONE=y/g' $DEFCONFIG
          
          # 2. ĮJUNGIAME Docker reikalavimus
          {
            echo "CONFIG_LTO_NONE=y"
            echo "CONFIG_WERROR=n"
            echo "CONFIG_OVERLAY_FS=y"
            echo "CONFIG_CGROUP_BPF=y"
            echo "CONFIG_MEMCG_SWAP=y"
            echo "CONFIG_MEMCG_SWAP_ENABLED=y"
            
            # 3. ĮJUNGIAME USB Serial (Zigbee lazdelėms)
            echo "CONFIG_USB_SERIAL=y"
            echo "CONFIG_USB_SERIAL_GENERIC=y"
            echo "CONFIG_USB_SERIAL_CP210X=y"
            echo "CONFIG_USB_SERIAL_CH341=y"
            echo "CONFIG_USB_SERIAL_FTDI_SIO=y"
            echo "CONFIG_USB_SERIAL_OPTION=y"
            
            # 4. ĮJUNGIAME USB diskų palaikymą (exFAT, NTFS)
            echo "CONFIG_USB_STORAGE=y"
            echo "CONFIG_USB_UAS=y"
            echo "CONFIG_EXFAT_FS=y"
            echo "CONFIG_NTFS_FS=y"
          } >> $DEFCONFIG

      - name: Build Kernel
        run: |
          cd kernel
          export PATH=$GITHUB_WORKSPACE/clang
